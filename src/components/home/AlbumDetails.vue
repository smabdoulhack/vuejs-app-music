<script setup>
import { ref } from "vue";
import { useRoute } from "vue-router";

// datas
import albums from "../../assets/data";

const route = useRoute();

const albumIndex = parseInt(route.params.index);
const album = albums[albumIndex];
</script>

<template>
  <main>
    <article class="album-card">
      <div class="card-header">
        <h4>{{ album.genre }}</h4>
      </div>
      <a href="/albums/3" class="title">{{ album.title }} </a>
      <div>
        <p class="description">{{ album.description }}</p>
        <p>duration: {{ album.duration.split(":")[0] }} minutes</p>
        <p>Like: {{ 5 }}</p>
      </div>
      <div class="card-footer">
        <ul>
          <li v-for="(tag, i) in album.tags" :key="i">{{ tag }}</li>
        </ul>
      </div>
      <p class="img-container">
        <img :src="album.coverImage" alt="" />
      </p>
    </article>
  </main>
</template>

<style scoped>
a {
  text-decoration: none;
}

.album-card {
  padding: 16px 8px;
  margin: 0 auto;
  text-align: left;
  color: #374151;
  background-color: #fff;
}

h4 {
  padding: 8px 0px;
  font-size: 14px;
  font-weight: 700;
  font-family: Roboto, sans-serif;
  background-color: #e9d5ff;
}

.title {
  font-size: 20px;
  font-weight: 700;
  color: #374151;
}

.title:hover {
  text-decoration: underline;
}

.description {
  padding-top: 15px;
  margin-bottom: 5px;
  font-size: 1rem;
}

ul {
  margin-top: 10px;
  display: flex;
  padding-left: 0;
}

ul li {
  list-style: none;
  padding: 4px 8px;
  margin-right: 20px;
  color: #fff;
  background: rgb(45 212 191);
}

.card-footer a {
  padding: 5px 8px;
  border: 0.5px solid rgba(0, 0, 0, 0.164);
}

.details-button {
  cursor: pointer;
}

.card-footer a:hover {
  color: #fff;
  background-color: #374151;
}

.card-footer p {
  margin: 10px 0;
  display: flex;
  gap: 10px;
}

.card-footer p span {
  display: flex;
  align-items: center;
}

.card-footer p svg {
  margin-right: 8px;
}

.img-container {
  margin: 15px auto;
  width: 800px;
  height: 500px;
  border: 1px solid blue;
}

.img-container img {
  width: 100%;
  height: 100%;
}
</style>